# Uppy 파일 업로드 UI 표시 안 되는 문제 해결

**날짜:** 2025-11-20  
**작성자:** GitHub Copilot  
**카테고리:** 버그 수정

---

## 📝 개요

온라인상담 글쓰기 화면에서 Uppy Dashboard UI가 전혀 표시되지 않고, 파일 드래그 앤 드롭도 작동하지 않는 문제 해결

---

## ❌ 문제점

### 증상
1. Uppy Dashboard가 화면에 표시되지 않음
2. "파일 선택" 버튼이 보이지 않음
3. 파일 드래그 앤 드롭 영역이 없음
4. 첨부파일 업로드 불가능

### 원인
**CSS 파일 누락**
- `uppy.min.css` 파일이 Uppy 브라우저 번들과 호환되지 않음
- 커스텀 구현에 맞는 CSS가 필요했으나 존재하지 않음
- Dashboard HTML이 렌더링되어도 스타일이 적용되지 않아 보이지 않음

---

## ✅ 해결 방법

### 1. Uppy 커스텀 CSS 생성

**파일:** `/src/main/resources/static/css/uppy/uppy-custom.css` (신규 생성, 159줄)

**주요 스타일:**

#### Dashboard 컨테이너
```css
.uppy-Dashboard {
  width: 100%;
  border: 2px dashed #dee2e6;
  border-radius: 0.5rem;
  background-color: #f8f9fa;
  transition: all 0.3s ease;
}
```

#### 드래그 오버 상태
```css
.uppy-Dashboard-inner--isDraggingOver {
  border-color: #0d6efd;
  background-color: #e7f1ff;
}
```

#### 파일 선택 버튼
```css
.uppy-Dashboard-browse {
  background-color: #0d6efd;
  color: white;
  border: none;
  padding: 0.5rem 1.5rem;
  border-radius: 0.375rem;
  cursor: pointer;
  font-size: 1rem;
}
```

#### 파일 목록 아이템
```css
.uppy-Dashboard-Item {
  display: flex;
  align-items: center;
  padding: 1rem;
  background-color: white;
  border: 1px solid #dee2e6;
  border-radius: 0.375rem;
  margin-bottom: 0.75rem;
}
```

### 2. counsel-write.html 수정

**변경 전:**
```html
<link rel="stylesheet" th:href="@{/css/uppy/uppy.min.css}">
```

**변경 후:**
```html
<link rel="stylesheet" th:href="@{/css/uppy/uppy-custom.css}">
```

### 3. uppy-browser.js 개선

#### Dashboard HTML 구조 개선
```javascript
var html = '<div class="uppy-Dashboard" style="min-height: ' + this.opts.height + 'px;">';
html += '  <div class="uppy-Dashboard-inner">';
html += '    <div class="uppy-Dashboard-innerWrap">';
html += '      <div class="uppy-Dashboard-dropFilesHereHint">';
html += '        📁 파일을 여기에 드롭하세요';
html += '      </div>';
html += '      <div class="uppy-DashboardContent-bar">';
html += '        <div class="uppy-DashboardContent-title">';
html += '          <div>파일을 드래그하거나 아래 버튼을 클릭하세요</div>';
html += '          <button type="button" class="uppy-Dashboard-browse">📎 파일 선택</button>';
html += '        </div>';
html += '      </div>';
html += '      <div class="uppy-Dashboard-files" role="list"></div>';
html += '    </div>';
html += '  </div>';
html += '</div>';
```

#### 파일 아이콘 추가
```javascript
var fileIcon = '📄';

// 파일 타입별 아이콘
if (file.type && file.type.startsWith('image/')) {
  fileIcon = '🖼️';
} else if (file.type === 'application/pdf') {
  fileIcon = '📑';
} else if (file.type && (file.type.includes('word') || file.name.endsWith('.doc'))) {
  fileIcon = '📝';
} else if (file.name.endsWith('.zip')) {
  fileIcon = '📦';
}
```

#### 디버깅 로그 추가
```javascript
console.log('Uppy Dashboard HTML inserted into:', this.opts.target);
console.log('Rendered', files.length, 'files in Dashboard');
```

### 4. JavaScript 초기화 코드 개선

**counsel-write.html에 디버깅 로그 추가:**
```javascript
// Uppy 객체 확인
console.log('Uppy object:', typeof Uppy);
console.log('Uppy.Core:', typeof Uppy.Core);
console.log('Uppy.Dashboard:', typeof Uppy.Dashboard);

const uppy = new Uppy.Core({...});
console.log('Uppy instance created:', uppy);

uppy.use(Uppy.Dashboard, {...});
console.log('Uppy Dashboard plugin registered');
```

---

## 📊 수정된 파일

| 파일 | 변경 내용 | 크기 |
|------|----------|------|
| **uppy-custom.css** | 신규 생성 (Uppy 커스텀 스타일) | 159줄 |
| **counsel-write.html** | CSS 파일 변경 + 디버깅 로그 추가 | 2곳 |
| **uppy-browser.js** | Dashboard HTML 개선 + 파일 아이콘 추가 | 3곳 |

**총 3개 파일 (1개 신규 생성, 2개 수정)**

---

## 🎨 UI 개선 사항

### 1. Dashboard 영역
```
┌─────────────────────────────────────────┐
│                                         │
│   📁 파일을 여기에 드롭하세요           │
│   (드래그 오버 시에만 표시)             │
│                                         │
│   파일을 드래그하거나 아래 버튼을       │
│   클릭하세요                            │
│                                         │
│   [ 📎 파일 선택 ]  ← 파일 선택 버튼   │
│                                         │
│   아직 선택된 파일이 없습니다.          │
│                                         │
└─────────────────────────────────────────┘
```

### 2. 파일 선택 후
```
┌─────────────────────────────────────────┐
│                                         │
│   [ 📎 파일 선택 ]                      │
│                                         │
│   ┌───────────────────────────────┐    │
│   │ 🖼️  example.jpg          [ × ]│    │
│   │     1.25 MB                   │    │
│   │     ▓▓▓▓▓▓░░░░░░░░ 50%        │    │
│   └───────────────────────────────┘    │
│                                         │
│   ┌───────────────────────────────┐    │
│   │ 📑  document.pdf         [ × ]│    │
│   │     0.85 MB                   │    │
│   │     ▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 100%       │    │
│   └───────────────────────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

### 3. 드래그 오버 상태
```
┌═════════════════════════════════════════┐
║                                         ║
║   📁 파일을 여기에 드롭하세요           ║
║   (파란색 배경, 파란색 테두리)          ║
║                                         ║
║                                         ║
║                                         ║
║                                         ║
║                                         ║
└═════════════════════════════════════════┘
```

---

## 🔍 테스트 시나리오

### 1. 파일 선택 버튼
```
✅ 1. /counsel/write 접속
✅ 2. "📎 파일 선택" 버튼 표시
✅ 3. 버튼 클릭 → 파일 선택 다이얼로그
✅ 4. 파일 선택 (최대 5개, 최대 5MB)
✅ 5. 파일 목록에 추가
✅ 6. 파일 아이콘 표시 (타입별)
```

### 2. 드래그 앤 드롭
```
✅ 1. /counsel/write 접속
✅ 2. 파일을 Dashboard 영역으로 드래그
✅ 3. 드래그 오버 시 배경색/테두리 변경
✅ 4. "파일을 여기에 드롭하세요" 메시지 표시
✅ 5. 파일 드롭
✅ 6. 파일 목록에 추가
```

### 3. 파일 제거
```
✅ 1. 파일 선택
✅ 2. 파일 목록 표시
✅ 3. "×" 버튼 클릭
✅ 4. 파일 목록에서 제거
✅ 5. "아직 선택된 파일이 없습니다." 메시지 표시
```

### 4. 파일 타입별 아이콘
```
✅ 이미지 파일 (jpg, png, gif 등): 🖼️
✅ PDF 파일: 📑
✅ Word 문서 (doc, docx): 📝
✅ ZIP 파일: 📦
✅ 기타 파일: 📄
```

---

## 🐛 해결된 문제

### Before (문제 상황)
- ❌ Dashboard UI 전혀 표시 안 됨
- ❌ 파일 선택 버튼 없음
- ❌ 드래그 앤 드롭 영역 없음
- ❌ 첨부파일 업로드 불가능
- ❌ 사용자가 어떻게 파일을 업로드하는지 알 수 없음

### After (해결 후)
- ✅ Dashboard UI 정상 표시
- ✅ 파일 선택 버튼 명확히 표시
- ✅ 드래그 앤 드롭 영역 시각적 피드백 제공
- ✅ 파일 목록 실시간 표시
- ✅ 파일 타입별 아이콘으로 직관적 UI
- ✅ 진행률 표시
- ✅ 파일 제거 기능

---

## 🎓 기술적 의의

### 1. CSS의 중요성
- 기능이 구현되어도 CSS가 없으면 UI가 표시되지 않음
- 사용자 경험(UX)의 핵심은 시각적 피드백
- 일관된 스타일 시스템 필요

### 2. 직관적인 UI 설계
```javascript
// 명확한 메시지
'파일을 드래그하거나 아래 버튼을 클릭하세요'

// 시각적 피드백
border-color: #0d6efd;  // 드래그 오버 시
background-color: #e7f1ff;

// 파일 타입별 아이콘
🖼️ 이미지, 📑 PDF, 📝 문서, 📦 압축 파일
```

### 3. 디버깅 로그의 중요성
```javascript
console.log('Uppy Dashboard HTML inserted into:', this.opts.target);
console.log('Rendered', files.length, 'files in Dashboard');
```
- 문제 발생 시 빠른 원인 파악
- 개발자가 실시간으로 상태 확인 가능

---

## 🚀 향후 개선 방향

### 1. 이미지 미리보기
```javascript
if (file.type.startsWith('image/')) {
  const reader = new FileReader();
  reader.onload = (e) => {
    // 썸네일 이미지 표시
    img.src = e.target.result;
  };
  reader.readAsDataURL(file.data);
}
```

### 2. 파일 크기 제한 안내
```javascript
// 파일 크기 초과 시 명확한 메시지
if (file.size > maxFileSize) {
  alert('파일 크기는 최대 5MB까지 업로드할 수 있습니다.');
}
```

### 3. 업로드 상태 표시
```javascript
// 업로드 중, 완료, 실패 상태 표시
<span class="badge bg-primary">업로드 중</span>
<span class="badge bg-success">완료</span>
<span class="badge bg-danger">실패</span>
```

---

## 📚 참고 문서

- [Uppy Dashboard Plugin](https://uppy.io/docs/dashboard/)
- [CSS Flexbox Layout](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout)
- [Bootstrap Utilities](https://getbootstrap.com/docs/5.3/utilities/)
- [2025-11-20-uppy-local-bundle-implementation.md](./2025-11-20-uppy-local-bundle-implementation.md)

---

**작성 시간:** 30분  
**난이도:** ★★☆☆☆  
**중요도:** ★★★★★

