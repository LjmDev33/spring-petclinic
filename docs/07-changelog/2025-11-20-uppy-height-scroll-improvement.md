# Uppy 첨부파일 영역 UI 개선 - 높이 축소 및 스크롤 추가

**날짜:** 2025-11-20  
**작성자:** GitHub Copilot  
**카테고리:** UI 개선

---

## 📝 개요

온라인상담 글쓰기 화면의 첨부파일 영역이 너무 커서 화면을 많이 차지하는 문제를 해결하고, 파일이 많을 때 스크롤 가능하도록 개선

---

## ❌ 문제점

### Before (개선 전)
```
첨부파일 영역 높이: 300px (고정)
파일 목록: 무제한 높이
└─> 파일 10개 이상 시 화면 밖으로 넘침
└─> 스크롤 없음
└─> 하단 버튼이 화면 밖으로 밀려남
```

### 증상
1. 첨부파일 영역이 너무 높아서 화면을 많이 차지
2. 파일이 많으면 목록이 화면 밖으로 넘침
3. 스크롤 기능 없음
4. "작성완료" 버튼이 화면 밖으로 밀려남

---

## ✅ 해결 방법

### 1. CSS 개선 - 높이 및 스크롤 추가

#### Dashboard 컨테이너
```css
.uppy-Dashboard {
  max-height: 400px;        /* 최대 높이 제한 */
  overflow: hidden;         /* 오버플로우 숨김 */
  display: flex;            /* Flexbox 레이아웃 */
  flex-direction: column;   /* 세로 방향 */
}
```

#### Dashboard 내부
```css
.uppy-Dashboard-inner {
  padding: 1.5rem;          /* 2rem → 1.5rem */
  flex-shrink: 0;           /* 축소 방지 */
}
```

#### 파일 목록 (스크롤 영역)
```css
.uppy-Dashboard-files {
  margin-top: 1rem;         /* 1.5rem → 1rem */
  max-height: 250px;        /* 최대 높이 250px */
  overflow-y: auto;         /* 세로 스크롤 */
  overflow-x: hidden;       /* 가로 스크롤 숨김 */
  padding-right: 0.5rem;    /* 스크롤바 여백 */
}
```

#### 커스텀 스크롤바
```css
.uppy-Dashboard-files::-webkit-scrollbar {
  width: 8px;
}

.uppy-Dashboard-files::-webkit-scrollbar-track {
  background: #f1f1f1;
  border-radius: 4px;
}

.uppy-Dashboard-files::-webkit-scrollbar-thumb {
  background: #888;
  border-radius: 4px;
}

.uppy-Dashboard-files::-webkit-scrollbar-thumb:hover {
  background: #555;
}
```

### 2. 여백 축소

#### Content Bar
```css
.uppy-DashboardContent-bar {
  margin-bottom: 1rem;      /* 1.5rem → 1rem */
}

.uppy-DashboardContent-title {
  font-size: 1rem;          /* 1.1rem → 1rem */
  margin-bottom: 0.75rem;   /* 1rem → 0.75rem */
}
```

#### 파일 아이템
```css
.uppy-Dashboard-Item {
  padding: 0.75rem;         /* 1rem → 0.75rem */
  margin-bottom: 0.5rem;    /* 0.75rem → 0.5rem */
}

.uppy-Dashboard-Item-preview {
  width: 40px;              /* 50px → 40px */
  height: 40px;             /* 50px → 40px */
  margin-right: 0.75rem;    /* 1rem → 0.75rem */
  flex-shrink: 0;           /* 아이콘 크기 고정 */
}

.uppy-Dashboard-Item-previewIcon {
  font-size: 1.25rem;       /* 1.5rem → 1.25rem */
}
```

#### 빈 상태 메시지
```css
.uppy-Dashboard-empty {
  padding: 1rem;            /* 2rem → 1rem */
  font-size: 0.9rem;        /* 0.95rem → 0.9rem */
}
```

### 3. JavaScript 높이 설정 변경

#### uppy-browser.js
```javascript
// Dashboard 기본 높이
height: 200,  // 550 → 200
```

#### counsel-write.html
```javascript
uppy.use(Uppy.Dashboard, {
  height: 200,  // 300 → 200
});
```

---

## 📊 수정된 파일

| 파일 | 변경 내용 | 변경 라인 |
|------|----------|----------|
| **uppy-custom.css** | Dashboard 높이 제한 및 Flexbox | 7-13 |
| **uppy-custom.css** | 파일 목록 스크롤 추가 | 42-59 |
| **uppy-custom.css** | 여백 축소 (Content Bar) | 33-40 |
| **uppy-custom.css** | 파일 아이템 크기 축소 | 61-88 |
| **uppy-custom.css** | 빈 상태 메시지 축소 | 137-142 |
| **uppy-browser.js** | 기본 높이 200px로 변경 | 217 |
| **counsel-write.html** | Dashboard 높이 200px | 155 |

**총 3개 파일, 7곳 수정**

---

## 🎨 UI 변화

### Before (개선 전)
```
┌────────────────────────────────────┐
│                                    │
│   파일을 드래그하거나              │
│   아래 버튼을 클릭하세요           │
│                                    │
│   [ 📎 파일 선택 ]                 │
│                                    │
│   ┌──────────────────────────┐    │
│   │ 🖼️ file1.jpg        [×] │    │
│   └──────────────────────────┘    │
│   ┌──────────────────────────┐    │
│   │ 📑 file2.pdf        [×] │    │
│   └──────────────────────────┘    │
│   ... (10개 이상 시 화면 밖)      │
│                                    │
│                                    │
│                                    │  ← 너무 높음
└────────────────────────────────────┘
```

### After (개선 후)
```
┌────────────────────────────────────┐
│  파일을 드래그하거나 버튼 클릭     │
│  [ 📎 파일 선택 ]                  │
│  ┌──────────────────────────┐ ↑   │
│  │ 🖼️ file1.jpg       [×]  │ │   │
│  ├──────────────────────────┤ │   │
│  │ 📑 file2.pdf       [×]  │ │   │
│  ├──────────────────────────┤ │   │
│  │ 📝 file3.doc       [×]  │ │   │
│  ├──────────────────────────┤ │   │
│  │ 📦 file4.zip       [×]  │ │   │
│  ├──────────────────────────┤ ↓   │
│  └──────────────────────────┘     │  ← 높이 제한
└────────────────────────────────────┘
    ↑
    스크롤 가능 (최대 250px)
```

---

## 📏 크기 변화

### 높이 비교
```
Dashboard 전체:
  Before: min-height: 300px
  After:  max-height: 400px (제한)

파일 목록:
  Before: 무제한 (파일 수만큼 증가)
  After:  max-height: 250px (스크롤)

패딩/여백:
  Dashboard-inner: 2rem → 1.5rem
  Content-bar: 1.5rem → 1rem
  Item: 1rem → 0.75rem
  Item margin: 0.75rem → 0.5rem
```

### 아이콘 크기
```
파일 아이콘:
  Before: 50×50px, font-size: 1.5rem
  After:  40×40px, font-size: 1.25rem
```

---

## 🔍 스크롤 동작

### 파일 개수별 동작
```
파일 1-3개:
  └─> 스크롤 없음 (목록 높이 < 250px)

파일 4-6개:
  └─> 스크롤 시작 (목록 높이 ≈ 250px)

파일 7-10개:
  └─> 스크롤 필요 (목록 높이 > 250px)

파일 11개 이상:
  └─> 스크롤 필수 (목록 높이 >> 250px)
```

### 스크롤바 스타일
```
너비: 8px
Track: #f1f1f1 (밝은 회색)
Thumb: #888 (회색)
Hover: #555 (어두운 회색)
모서리: 4px 둥근 처리
```

---

## ✨ 개선 효과

### 1. 화면 공간 효율성
```
Before:
  └─> 첨부파일 영역이 화면의 40% 이상 차지
  └─> 하단 버튼이 보이지 않음

After:
  └─> 첨부파일 영역이 화면의 20-25% 차지
  └─> 하단 버튼이 항상 보임
```

### 2. 사용자 경험
```
Before:
  └─> 파일 10개 추가 시 스크롤이 화면 끝까지
  └─> "작성완료" 버튼 찾기 어려움

After:
  └─> 파일 몇 개든 영역 크기 일정
  └─> "작성완료" 버튼 항상 접근 가능
```

### 3. 직관적인 UI
```
✅ 스크롤바로 "더 많은 파일이 있음"을 알 수 있음
✅ 컴팩트한 디자인으로 전문적인 느낌
✅ 빠른 파일 확인 및 제거 가능
```

---

## 🧪 테스트 시나리오

### 1. 파일 1-3개
```
✅ 1. 파일 3개 선택
✅ 2. 스크롤바 표시 안 됨
✅ 3. 모든 파일 한눈에 보임
✅ 4. 영역 높이: 약 180px
```

### 2. 파일 4-6개
```
✅ 1. 파일 6개 선택
✅ 2. 스크롤바 표시 시작
✅ 3. 약간의 스크롤 필요
✅ 4. 영역 높이: 약 250px (max)
```

### 3. 파일 10개 이상
```
✅ 1. 파일 15개 선택
✅ 2. 스크롤바 활성화
✅ 3. 위아래로 스크롤 가능
✅ 4. 영역 높이: 250px (고정)
✅ 5. 하단 버튼 항상 보임
```

### 4. 스크롤바 동작
```
✅ 1. 마우스 휠로 스크롤
✅ 2. 스크롤바 드래그
✅ 3. 스크롤바 Hover 시 색상 변경
✅ 4. 부드러운 스크롤 애니메이션
```

---

## 🎓 기술적 의의

### 1. Flexbox 활용
```css
display: flex;
flex-direction: column;
flex-shrink: 0;  /* 헤더/버튼 영역 고정 */
```
- 파일 목록만 스크롤 영역으로 분리
- 헤더와 버튼은 항상 보임

### 2. 오버플로우 제어
```css
overflow-y: auto;     /* 필요 시 스크롤 */
overflow-x: hidden;   /* 가로 스크롤 방지 */
```
- 세로 스크롤만 허용
- 레이아웃 깨짐 방지

### 3. WebKit 스크롤바 커스터마이징
```css
::-webkit-scrollbar
::-webkit-scrollbar-track
::-webkit-scrollbar-thumb
```
- 브라우저 기본 스크롤바 대체
- 디자인 일관성 유지

---

## 🚀 향후 개선 방향

### 1. 가상 스크롤 (Virtual Scroll)
```javascript
// 수백 개 파일도 부드럽게 렌더링
// 실제 DOM에는 보이는 영역만 렌더링
if (files.length > 100) {
  renderVirtualScroll(files);
}
```

### 2. 파일 검색 기능
```html
<input type="text" placeholder="파일 이름 검색...">
<!-- 파일 목록 필터링 -->
```

### 3. 파일 정렬 기능
```html
<select>
  <option>이름순</option>
  <option>크기순</option>
  <option>추가순</option>
</select>
```

### 4. 일괄 선택/제거
```html
<button>전체 선택</button>
<button>선택 항목 제거</button>
```

---

## 📚 참고 문서

- [CSS Flexbox Layout](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout)
- [CSS Overflow Property](https://developer.mozilla.org/en-US/docs/Web/CSS/overflow)
- [WebKit Scrollbar Styling](https://developer.mozilla.org/en-US/docs/Web/CSS/::-webkit-scrollbar)
- [2025-11-20-uppy-ui-display-fix.md](./2025-11-20-uppy-ui-display-fix.md)

---

**작성 시간:** 15분  
**난이도:** ★☆☆☆☆  
**중요도:** ★★★★☆

